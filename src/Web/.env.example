# Public development configuration only. Never put signing keys, database
# passwords, or other secrets here.
#
# Leave empty to use the Vite dev-server proxy (same origin as the UI).
# Set to the Host origin (e.g. http://localhost:5227) if you are not using the proxy.
VITE_API_BASE_URL=
