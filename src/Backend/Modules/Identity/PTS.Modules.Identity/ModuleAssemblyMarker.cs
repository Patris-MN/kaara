namespace PTS.Modules.Identity;

/// <summary>
/// Non-functional marker type used to obtain a reference to this module's assembly
/// (e.g. from architecture tests or module-discovery code) without depending on any
/// concrete type inside the module.
/// </summary>
public static class ModuleAssemblyMarker
{
}
