namespace PTS.SharedKernel;

/// <summary>
/// Non-functional marker type used to obtain a reference to this assembly
/// (e.g. from architecture tests) without depending on any concrete type.
/// </summary>
public static class ModuleAssemblyMarker
{
}
